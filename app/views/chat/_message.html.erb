<span class="chat_time"><%= message.created_at.strftime("%H:%M:%S") %> </span><span class="from-user<%= from_user message %>"><%= link_to_function message.get_from_user, "say('#{message.get_from_user}')" %></span><% if message.get_to_user.present? %> : <span class="to-user<%= to_user message %>"> <%= link_to_function message.get_to_user, "say('#{message.get_to_user}')" %> </span><% end %> <span class="message"><%= message.message %></span><br>